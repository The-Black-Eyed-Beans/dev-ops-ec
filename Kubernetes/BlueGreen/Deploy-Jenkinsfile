pipeline {
    agent any

    environment {
        AWS_REGION = credentials("AWS_REGION")
        AWS_ID = credentials("AWS_ID")
        COMMIT_HASH = "latest"
    }

    stages {
        stage("Test") {
            steps {
                script{
                    withEnv(["DEVELOPMENT_COLOR=green"]){
                        dir('Kubernetes/BlueGreen'){
                            sh "envsubst < aline-bank.yaml | kubectl apply -f -"
                        }
                    }
                }
            }
        }
    }
}