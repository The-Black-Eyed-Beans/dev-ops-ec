apiVersion: apps/v1
kind: Deployment
metadata:
  name: aline-trans-deployment-${DEPLOYMENT_COLOR}
  namespace: afs-ec
  labels:
    app: aline-trans-${DEPLOYMENT_COLOR}
spec:
  replicas: ${TRANS_REPLICAS}
  selector:
    matchLabels:
      app: aline-trans-${DEPLOYMENT_COLOR}
  template:
    metadata:
      labels:
        app: aline-trans-${DEPLOYMENT_COLOR}
        role: microservice
        color: ${DEPLOYMENT_COLOR}
    spec:
      containers:
      - name: aline-trans-ms
        image: ${AWS_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/transaction-microservice-ec:${COMMIT_HASH}
        ports:
        - name: trans-${DEPLOYMENT_COLOR}
          containerPort: 8073
        envFrom:
          - configMapRef:
              name: service-config
          - secretRef:
              name: service-secret
        env:
          - name: APP_PORT
            value: "8073"
      imagePullSecrets:
        - name: regcred